% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper.R
\name{compare_mixtures}
\alias{compare_mixtures}
\title{Compare Mixture Models Across a Range of Class Numbers}
\usage{
compare_mixtures(
  X,
  k_range = 1:5,
  measurement = "binary",
  n_init = 10,
  n_steps = 1,
  ...
)
}
\arguments{
\item{X}{A numeric matrix or data frame of indicator variables.}

\item{k_range}{Integer vector of class numbers to fit. All values must be >= 1. Default is \code{1:5}.}

\item{measurement}{Character string specifying the measurement model type.
See \code{\link{fit_mixture}} for accepted values. Default is
\code{"binary"}.}

\item{n_init}{Positive integer. Number of random restarts per model.
Default is \code{10}.}

\item{n_steps}{Integer. Estimation method: \code{1}, \code{2}, or \code{3}.
Default is \code{1}.}

\item{...}{Additional arguments passed to \code{\link{fit_mixture}}.}
}
\value{
A named list with three elements:
\itemize{
\item \code{fit_table} Data frame with one row per K and columns \code{Classes}, \code{LL},
\code{Params}, \code{AIC}, \code{BIC}, \code{SABIC}, \code{Entropy}.
\item \code{models} Named list of fitted \code{mixture_model} objects, one per K
(names are \code{"K1"}, \code{"K2"}, etc.).
\item \code{best_k} Integer. The value of K with the lowest BIC.
}
}
\description{
Fits a sequence of measurement-only mixture models, one for each value of
\code{k} in \code{k_range}, and returns a table of fit indices to guide
class enumeration. The best model according to BIC is identified
automatically.
}
\examples{
set.seed(1)
X <- matrix(rbinom(500, 1, 0.5), nrow = 100)
result <- compare_mixtures(X, k_range = 1:4, measurement = "binary",
                           n_init = 5)
result$fit_table
result$best_k

}
\references{
Nylund, K. L., Asparouhov, T., & Muthen, B. O. (2007). Deciding on the number
of classes in latent class analysis and growth mixture modeling: A Monte
Carlo simulation study. \emph{Structural Equation Modeling}, \emph{14}(4),
535-569. \doi{10.1080/10705510701575396}

Masyn, K. E. (2013). Latent class analysis and finite mixture modeling. In
T. D. Little (Ed.), \emph{The Oxford Handbook of Quantitative Methods}
(Vol. 2, pp. 551-611). Oxford University Press.
}
