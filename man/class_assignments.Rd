% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lta_methods.R, R/wrapper.R
\name{class_assignments.lta_model}
\alias{class_assignments.lta_model}
\alias{class_assignments}
\alias{class_assignments.default}
\title{Class Assignments for Each Case}
\usage{
\method{class_assignments}{lta_model}(
  object,
  type = c("modal", "posterior", "both", "viterbi"),
  occasion = NULL,
  ...
)

class_assignments(object, type = c("modal", "posterior", "both"), ...)

\method{class_assignments}{default}(object, type = c("modal", "posterior", "both"), ...)
}
\arguments{
\item{object}{A fitted model: a \code{mixture_model} (including the growth
models) or an \code{lta_model}.}

\item{type}{What to return. \code{"modal"} (default) gives the assigned
class; \code{"posterior"} the full matrix of posterior probabilities;
\code{"both"} a data frame carrying the assignment, its probability, and
the posterior columns; \code{"viterbi"}, for an \code{lta_model} only,
globally decodes the single most probable status sequence (see Details).}

\item{occasion}{For an \code{lta_model}, the index of a single occasion. Omit for
every occasion, which \code{type = "both"} does not support.}

\item{...}{Passed to methods.}
}
\value{
For \code{"modal"}, an integer vector of length n. For
\code{"posterior"}, an n-by-K matrix with the class labels as column names.
For \code{"both"}, a data frame with \code{class}, \code{probability} (the
assigned class's posterior probability, i.e. a per-case classification
certainty), and then the K posterior columns.
}
\description{
The class each case is assigned to, and the posterior probabilities behind
that assignment. This is the accessor for the per-case classification, so
that reaching it does not mean indexing into the fitted object's internals.
}
\details{
For a latent transition model the assignment is of latent \emph{status}, and a
status assignment is made at every occasion rather than once per case — the
same convention \code{\link[=status_prevalences]{status_prevalences()}} and the entropy in
\code{\link[=fit_lta]{fit_lta()}}'s \code{metrics} already follow. Supply \code{occasion} to work with one
occasion at the shape the mixture methods return; omit it for all of them at
once. To assign the latent \emph{class} of a mixture latent Markov model, use
\code{object$class_posterior}.

\code{type = "viterbi"} decodes globally instead: it returns the single most
probable \emph{sequence} of statuses across all occasions at once, rather than
the most probable status at each occasion taken separately. The two can
disagree — the occasion-by-occasion assignment above may string together a
sequence the model itself gives zero probability, if a locally-favoured
status at one occasion can only be reached by a transition the model
forbids or scores as unlikely, while Viterbi decoding can never do that. Its
\code{occasion} argument still selects one occasion's column of that path; it
does not change which path is decoded. With \code{n_classes} > 1 the class and
the path are decoded jointly, and the class each case was assigned to is
available as \code{attr(result, "class_assigned")}.

Modal class assignment discards classification error. Do not use the returned
class as though it were an observed variable in a subsequent regression,
ANOVA or t-test: doing so attenuates the association, severely when the
classes are not well separated (Bolck, Croon & Hagenaars, 2004; Vermunt,
2010; Bakk, Tekle & Vermunt, 2013). Use \code{\link[=add_covariates]{add_covariates()}} and
\code{\link[=add_outcome]{add_outcome()}}, which correct for it. This function is for plotting,
exporting and describing a solution.
}
\examples{
set.seed(1)
X   <- matrix(rbinom(500, 1, 0.5), nrow = 100)
fit <- fit_mixture(X, n_classes = 2, measurement = "binary")
table(class_assignments(fit))
head(class_assignments(fit, "both"))
# To relate the classes to an external variable, do not regress on the
# assigned class - use the bias-adjusted third step instead:
# add_outcome(fit, y)

}
\references{
Bolck, A., Croon, M., & Hagenaars, J. (2004). Estimating latent structure
models with categorical variables: One-step versus three-step estimators.
\emph{Political Analysis}, \emph{12}(1), 3–27. \doi{10.1093/pan/mph001}

Vermunt, J. K. (2010). Latent class modeling with covariates: Two improved
three-step approaches. \emph{Political Analysis}, \emph{18}(4), 450–469.
\doi{10.1093/pan/mpq025}

Bakk, Z., Tekle, F. B., & Vermunt, J. K. (2013). Estimating the association
between latent class membership and external variables using bias-adjusted
three-step approaches. \emph{Sociological Methodology}, \emph{43}(1),
272–311. \doi{10.1177/0081175012470644}
}
\seealso{
\code{\link[=class_sizes]{class_sizes()}}, \code{\link[=classification_table]{classification_table()}},
\code{\link[=classification_diagnostics]{classification_diagnostics()}}.
}
