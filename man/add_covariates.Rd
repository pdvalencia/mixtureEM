% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_structural.R
\name{add_covariates}
\alias{add_covariates}
\title{Examine Predictors of Class Membership on a Fitted Model}
\usage{
add_covariates(
  fit,
  predictors,
  correction = c("ML", "BCH", "none"),
  se = c("corrected", "robust", "hessian"),
  max_iter = 1000,
  ...
)
}
\arguments{
\item{fit}{A fitted unconditional model from \code{\link[=fit_mixture]{fit_mixture()}} (or
\code{\link[=fit_rmlca]{fit_rmlca()}}, \code{\link[=fit_lcga]{fit_lcga()}}, \code{\link[=fit_gmm]{fit_gmm()}}).}

\item{predictors}{Covariates that predict class membership: a data frame,
matrix, or single vector/factor. Factors are dummy-coded with the first
level as reference. Must have one row per case of the data the model was
fit to.}

\item{correction}{Bias correction for the third step: \code{"ML"} (default;
Vermunt, 2010), \code{"BCH"}, or \code{"none"}.}

\item{se}{Standard-error estimator passed on to the third step:
\code{"corrected"} (default), \code{"robust"}, or \code{"hessian"}.}

\item{max_iter}{Maximum iterations for the step-3 estimation.}

\item{...}{Currently unused.}
}
\value{
A \code{mixture_model} with the class-membership regression attached.
Use \code{\link[=summary]{summary()}} for odds ratios and omnibus tests; \code{\link[=coef]{coef()}},
\code{\link[=confint]{confint()}}, and \code{\link[=wald_omnibus_test]{wald_omnibus_test()}} also apply.
}
\description{
Takes the latent class model you have already chosen and relates covariates
to class membership with the bias-adjusted three-step approach (Vermunt,
2010). The measurement model is reused exactly as fitted — no re-estimation,
and no risk of landing on a different solution — so this is both faster and
conceptually cleaner than re-specifying the model with \code{predictors}.
}
\examples{
set.seed(1)
items <- matrix(rbinom(300, 1, 0.5), nrow = 100)
age   <- rnorm(100)
fit   <- fit_mixture(items, n_classes = 2)
fit_cov <- add_covariates(fit, age)
summary(fit_cov)

}
\references{
Vermunt, J. K. (2010). Latent class modeling with covariates: Two improved
three-step approaches. \emph{Political Analysis}, \emph{18}(4), 450–469.
\doi{10.1093/pan/mpq025}

Bakk, Z., Oberski, D. L., & Vermunt, J. K. (2014). Relating latent class
assignments to external variables: Standard errors for correct inference.
\emph{Political Analysis}, \emph{22}(4), 520–540. \doi{10.1093/pan/mpu003}
}
\seealso{
\code{\link[=add_outcome]{add_outcome()}} for distal outcomes; \code{\link[=fit_mixture]{fit_mixture()}} to fit the
unconditional model.
}
