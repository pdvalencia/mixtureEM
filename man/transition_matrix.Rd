% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lta_methods.R
\name{transition_matrix}
\alias{transition_matrix}
\title{Transition Probability Matrices}
\usage{
transition_matrix(object, occasion = NULL, class = NULL)
}
\arguments{
\item{object}{An object returned by \code{\link[=fit_lta]{fit_lta()}}.}

\item{occasion}{Optional index of a single transition (1 for the move from
occasion 1 to occasion 2, and so on). Omit to get them all.}

\item{class}{Optional latent class, for a model fitted with \code{n_classes} > 1
or \code{mover_stayer = TRUE}. Omit to get every class.}
}
\value{
A matrix, or a named list of matrices when \code{occasion} is omitted and
the transitions are time-heterogeneous, nested inside a list over classes
when the model has more than one.
}
\description{
Returns the estimated transition probabilities \eqn{\tau_{s_t | s_{t-1}}}: the
probability of occupying each latent status at one occasion given the status
held at the previous one. Rows are the origin status and sum to one; the
diagonal is the probability of staying put.

There is one matrix per pair of adjacent occasions unless the model was fitted
with \code{tau_homogeneous = TRUE}, in which case a single matrix is shared.

In a mixture latent Markov model each latent class has its own transitions,
so the result gains an outer level indexed by class; \code{class} picks one out.
}
