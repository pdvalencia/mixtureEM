% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lta_methods.R
\name{plot.lta_model}
\alias{plot.lta_model}
\title{Plots for a Fitted Latent Transition Model}
\usage{
\method{plot}{lta_model}(
  x,
  type = c("prevalence", "transitions", "profiles"),
  main = NULL,
  status_labels = NULL,
  colors = NULL,
  class = NULL,
  ...
)
}
\arguments{
\item{x}{An object returned by \code{\link[=fit_lta]{fit_lta()}}.}

\item{type}{Which view to draw.}

\item{main}{Plot title.}

\item{status_labels}{Optional labels for the latent statuses.}

\item{colors}{Optional colour vector, recycled across statuses.}

\item{class}{Optional latent class, for a model fitted with \code{n_classes} > 1
or \code{mover_stayer = TRUE}.}

\item{...}{Ignored.}
}
\value{
\code{x}, invisibly.
}
\description{
Three views, all in base graphics:
\describe{
\item{\code{"prevalence"}}{latent status prevalence across occasions - the
summary of where people are over time;}
\item{\code{"transitions"}}{a shaded matrix of transition probabilities, one
panel per pair of adjacent occasions, with the values printed in;}
\item{\code{"profiles"}}{item-response probabilities (or means) by status, which
is what the status labels rest on.}
}

With more than one latent class, \code{"prevalence"} draws one panel per class -
the classic longitudinal profile plot, in which a stayer class is the
flat one - and \code{"transitions"} one panel per class per pair of occasions.
\code{class} restricts either to a single class.
}
