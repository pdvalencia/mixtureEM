% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outcome_contrasts.R
\name{outcome_contrasts}
\alias{outcome_contrasts}
\title{Class-vs-Class Contrasts on a Distal Outcome}
\usage{
outcome_contrasts(
  fit,
  ref = NULL,
  adjust = c("none", "holm", "bonferroni"),
  level = 0.95,
  ...
)
}
\arguments{
\item{fit}{A fitted model with a distal outcome attached, as returned by
\code{\link[=add_outcome]{add_outcome()}}.}

\item{ref}{Optional reference class, as an integer between 1 and the number
of classes. \code{NULL} (the default) reports all pairs.}

\item{adjust}{Multiplicity adjustment across the reported contrasts, passed
to \code{\link[stats:p.adjust]{stats::p.adjust()}}: \code{"none"} (the default), \code{"holm"} or
\code{"bonferroni"}. With \code{"none"} the p-values are the per-contrast ones, which
is what to report when the contrasts were named in advance; an adjustment
belongs on the all-pairs table read as a family.}

\item{level}{Confidence level for the intervals. Default \code{0.95}.}

\item{...}{Currently unused.}
}
\value{
A data frame of class \code{outcome_contrasts}, one row per contrast, with
columns \code{category} (the outcome category the contrast is on, \code{NA} for a
continuous outcome), \code{class}, \code{reference}, \code{estimate}, \code{se}, \code{lower},
\code{upper}, \code{z} and \code{p}, plus \code{p_adj} when \code{adjust} is not \code{"none"} and
\code{OR}, \code{OR_lower}, \code{OR_upper} for a categorical outcome, whose estimates are
log odds. The \code{method} attribute names the covariance the standard errors
came from.
}
\description{
Tests which latent classes differ on a distal outcome attached by
\code{\link[=add_outcome]{add_outcome()}}, rather than whether any of them do. The omnibus Wald test
printed by \code{\link[=summary]{summary()}} is a single joint statistic; this is the set of
contrasts behind it, each with a standard error, a confidence interval and a
p-value.

Each contrast is reported as \code{class} minus \code{reference}, so a positive
estimate means the class scores above the class it is compared with. With
\code{ref = NULL} every pair of classes is reported once; naming a class in \code{ref}
holds it fixed as the comparison.

The standard errors account for the covariance between the class parameters,
which is not optional here. Two class means from one fit are estimated from
the same posteriors, and under the BCH correction from the same inverted
classification-error matrix, so they are correlated; the standard error of
their difference is not the root of the sum of their squared standard errors,
and the naive version is conservative exactly when the correlation is
positive. The full sandwich covariance the fit already carries is used
wherever it is available.
}
\examples{
set.seed(1)
items <- matrix(rbinom(300, 1, 0.5), nrow = 100)
bmi   <- rnorm(100, mean = 25)
fit   <- fit_mixture(items, n_classes = 2, measurement = "binary")
fit_out <- add_outcome(fit, bmi)
outcome_contrasts(fit_out)

}
\seealso{
\code{\link[=add_outcome]{add_outcome()}} for fitting the outcome model and \code{\link[=summary]{summary()}} for
the omnibus test; \code{\link[=wald_omnibus_test]{wald_omnibus_test()}} and \code{\link[=bootstrap_covariates]{bootstrap_covariates()}} for
the bootstrap route, which is what to use for an outcome whose parameters
are estimated one class at a time.
}
