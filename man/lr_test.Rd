% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lta_methods.R
\name{lr_test}
\alias{lr_test}
\title{Likelihood-Ratio Test for Two Nested Models}
\usage{
lr_test(restricted, full)
}
\arguments{
\item{restricted}{The more constrained model (fewer parameters).}

\item{full}{The less constrained model.}
}
\value{
A list of class \code{"lr_test"}.
}
\description{
Compares two nested models by the likelihood-ratio difference test,
\eqn{-2(\ell_0 - \ell_1)} on \eqn{P_1 - P_0} degrees of freedom. It accepts
any pair of fits from \code{\link[=fit_mixture]{fit_mixture()}}, \code{\link[=fit_rmlca]{fit_rmlca()}} or \code{\link[=fit_lta]{fit_lta()}},
and answers questions of the form "does freeing these parameters buy a
significantly better fit?"

\itemize{
\item \strong{Measurement invariance across groups} (Collins & Lanza, 2010, sec.
5.8): fit \code{\link[=fit_mixture]{fit_mixture()}} with \code{group_effects = "prevalence"} and with
\code{"both"}, and compare. This is a cross-sectional test.
\item \strong{Equal prevalences across groups} (sec. 5.11): compare
\code{group_effects = "none"} against \code{"prevalence"}.
\item \strong{Measurement invariance across time} (sec. 7.11): fit \code{\link[=fit_lta]{fit_lta()}}
with \code{measurement_invariance = "full"} and \code{"none"} and compare.
\item \strong{A time-homogeneous transition matrix} (sec. 7.14): fit
\code{\link[=fit_lta]{fit_lta()}} with \code{transition_invariance = "full"} and \code{"none"}.
}

The models must be nested and fitted to the same data. That is not checked
beyond the parameter counts and sample size, so it remains the analyst's
responsibility.

Because \code{full} strictly nests \code{restricted}, its log-likelihood can never
be genuinely lower — if it comes out that way here, the \code{full} model's
random-restart search landed on a worse local optimum than the
\code{restricted} model's did, not a real result. A warning is issued in that
case; refitting \code{full} with a larger \code{n_init} is the usual fix.
}
\references{
Collins, L. M., & Lanza, S. T. (2010). \emph{Latent Class and Latent
Transition Analysis: With Applications in the Social, Behavioral, and Health
Sciences}. Wiley.
}
