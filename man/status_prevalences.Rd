% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lta_methods.R
\name{status_prevalences}
\alias{status_prevalences}
\title{Latent Status Prevalences by Occasion}
\usage{
status_prevalences(object, type = c("model", "posterior"), class = NULL)
}
\arguments{
\item{object}{An object returned by \code{\link[=fit_lta]{fit_lta()}}.}

\item{type}{\code{"model"} (default) or \code{"posterior"}.}

\item{class}{Optional latent class, for a model fitted with \code{n_classes} > 1
or \code{mover_stayer = TRUE}.}
}
\value{
An occasions-by-statuses matrix.
}
\description{
The proportion in each latent status at each occasion. The model-implied
prevalences propagate \eqn{\delta} through the transition matrices; the
empirical ones average the posterior status probabilities.

With more than one latent class the whole-sample prevalences are returned by
default - the mixture's own marginal, which is what should be compared with
the observed proportions - and \code{class} picks out a single class's chain.
}
