% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lcga.R
\name{plot.lcga}
\alias{plot.lcga}
\title{Trajectory Plot for a Latent Class Growth Model}
\usage{
\method{plot}{lcga}(
  x,
  observed = c("means", "cases", "none"),
  main = NULL,
  class_labels = NULL,
  colors = NULL,
  ...
)
}
\arguments{
\item{x}{An object returned by \code{\link[=fit_lcga]{fit_lcga()}}.}

\item{observed}{What of the observed data to draw: \code{"means"} (the observed
mean at each occasion among cases modally assigned to the class, dotted),
\code{"cases"} (individual trajectories, translucent — informative for a
continuous or count outcome, much less so for a binary one) or \code{"none"}.}

\item{main}{Plot title.}

\item{class_labels}{Optional class labels for the legend.}

\item{colors}{Optional colour vector, recycled across classes.}

\item{...}{Ignored.}
}
\value{
\code{x}, invisibly.
}
\description{
Draws the estimated trajectory of each class on the response scale, with the
occasions on the x-axis — the figure an LCGA is read from, since the classes
\emph{are} the trajectories — and the observed data of the cases assigned to each
class behind them.
}
