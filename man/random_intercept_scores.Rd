% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lta_ri.R
\name{random_intercept_scores}
\alias{random_intercept_scores}
\title{Random-intercept factor scores}
\usage{
random_intercept_scores(fit)
}
\arguments{
\item{fit}{A model fitted by \code{\link[=fit_lta]{fit_lta()}} with \code{random_intercept} not
\code{"none"}.}
}
\value{
A data frame with one row per case in the fitted data.
}
\description{
The between-subject factor a random-intercept \code{fit_lta()} model estimates,
one row per case. \code{map_class} is the node at the posterior mode, and every
node's own posterior probability is returned as \code{node1_posterior},
\code{node2_posterior}, and so on. For \code{random_intercept = "continuous"}, where
the nodes are quadrature points on a single scale rather than unordered
classes, \code{mean_score} (the posterior mean) and \code{map_score} (the quadrature
node at the posterior mode) are added as well. When the fit used
\code{predictors_random_intercept}, \code{predicted_mean} (the regression-implied
factor mean, \verb{x_i'beta}) is added too, alongside the posterior \code{mean_score}.
}
