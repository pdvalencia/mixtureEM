% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm.R
\name{fit_gmm}
\alias{fit_gmm}
\title{Growth Mixture Modeling}
\usage{
fit_gmm(
  indicator,
  n_classes = 2,
  times = NULL,
  degree = 1,
  random_effects = c("intercept_slope", "intercept", "none", "all"),
  psi = c("equal", "free"),
  residual = c("occasion", "constant"),
  residual_equal = TRUE,
  time_scores = NULL,
  layout = c("time_major", "item_major"),
  id = NULL,
  time = NULL,
  item = NULL,
  time_labels = NULL,
  predictors = NULL,
  growth_predictors = NULL,
  growth_predictors_equal = TRUE,
  ...
)
}
\arguments{
\item{indicator}{The repeated outcome. Either a wide matrix or data frame
with one column per occasion, a three-dimensional array with dimensions n
by 1 by times, or a long data frame together with \code{id} and \code{time}. Exactly
one variable may be modelled.}

\item{n_classes}{Integer. Number of trajectory classes.}

\item{times}{Integer. Number of occasions. Required for wide input; inferred
otherwise.}

\item{degree}{Degree of the polynomial in time: \code{1} for a linear trajectory
(intercept and slope), \code{2} for a quadratic, and so on.}

\item{random_effects}{Which growth factors vary within a class:
\itemize{
\item \code{"intercept_slope"} (default) — both the intercept and the linear slope,
the standard growth mixture model. Cases in a class differ both in where
they start and in how fast they change.
\item \code{"intercept"} — the intercept only. Cases in a class start at different
levels but change in parallel.
\item \code{"none"} — no random effects, which is latent class growth analysis; the
same model as \code{fit_lcga(family = "gaussian")}, but with the residual
variances free over occasions by default.
\item \code{"all"} — every growth factor, including a quadratic or higher term.
}}

\item{psi}{Whether the growth-factor covariance \eqn{\Psi} is held \code{"equal"}
across classes (the default) or estimated \code{"free"}ly in each.
Equality is the better default in practice as well as by convention: it is
what BIC typically selects, and a class-specific \eqn{\Psi} is
where growth mixture models most often produce a negative variance.}

\item{residual}{Whether the residual variances are \code{"occasion"}-specific (the
default) or \code{"constant"} across occasions.}

\item{residual_equal}{Logical. Hold the residual variances equal across
classes (the conventional default).}

\item{time_scores}{Numeric values of time used in the polynomial, one per
occasion. Defaults to \verb{0, 1, ..., times - 1}, which makes the intercept
growth factor the level at the first occasion. Supply the actual
measurement times when the occasions are unequally spaced.}

\item{layout}{For wide input with a three-dimensional array or several
columns per occasion, whether columns run \code{"time_major"} or \code{"item_major"}.}

\item{id, time}{For long input, the case and occasion identifiers, given
either as column names or as vectors.}

\item{item}{For long input, the column holding the outcome.}

\item{time_labels}{Optional display labels for the occasions.}

\item{predictors}{Optional predictors of class membership (\verb{c ON x}), passed
to \code{\link[=fit_mixture]{fit_mixture()}}'s three-step machinery.}

\item{growth_predictors}{Optional covariates regressing the growth factors
themselves (\verb{i s ON x}): a vector, matrix or data frame with one row per
case, factors dummy-coded. These enter the measurement model rather than
the structural one, so they are estimated jointly with the trajectories in
a single pass regardless of \code{n_steps}. They must be complete: a case
missing a covariate has no model-implied mean, and unlike a missing
occasion there is nothing here to integrate it out, so remove or impute
those cases first.}

\item{growth_predictors_equal}{Logical. Hold the growth-factor regressions
equal across classes (the conventional default). Set \code{FALSE} to let each class have its
own, which asks whether a covariate matters differently in different
trajectory groups.}

\item{...}{Further arguments passed to \code{\link[=fit_mixture]{fit_mixture()}}, such as \code{outcome}
and its companions, \code{n_init}, \code{random_state}, \code{weights}, \code{strata} or
\code{cluster}.}
}
\value{
An object of class \code{c("gmm", "mixture_model")}. In addition to the
usual fields it carries \verb{$growth}, holding the design matrix, time scores,
the per-class growth-factor means, the fitted class trajectories, the
growth-factor covariance of each class, the residual variances and, when
\code{growth_predictors} were given, \verb{$growth$coefficients}: one
growth-factor-by-covariate matrix per class.
}
\description{
Fits class-specific growth curves to a continuous outcome measured
repeatedly, allowing cases to vary about their own class's trajectory through
random growth factors. This is the growth mixture model of Muthen & Shedden
(1999) and, with \code{random_effects = "none"}, latent
class growth analysis for a continuous outcome.

The model is
\deqn{y_i \mid c_i = k \sim N(\Lambda (\alpha_k + \Gamma_k x_i),\; \Lambda_r \Psi_k \Lambda_r' + \Theta_k),}
where \eqn{\Lambda} is the polynomial design in time, \eqn{\alpha_k} the
growth-factor means of class \eqn{k}, \eqn{\Psi_k} the covariance of the
growth factors that are allowed to vary within a class, and \eqn{\Theta_k}
the diagonal matrix of residual variances. Because the outcome is continuous
the random effects integrate out in closed form, so no numerical integration
is involved and the estimator is the ordinary EM algorithm the rest of the
package uses.

\eqn{\Gamma_k} is present only when \code{growth_predictors} are supplied, and is
the within-class growth-factor regression: covariates shift a case's own growth factors \emph{within} its
class, answering "who, in this trajectory group, starts higher or grows
faster?". It is a different question from \code{predictors}, which is \verb{c ON x} and
asks who is \emph{in} the group; the two can be asked together, and are then
estimated in one pass with \code{n_steps = 1}. With covariates
present \eqn{\alpha_k} is the growth-factor \emph{intercept} rather than its mean,
and the reported trajectory is evaluated at the sample mean of the
covariates.

Contrast \code{\link[=fit_lcga]{fit_lcga()}}, which fixes the growth-factor variances at zero, so
every case in a class sits on that class's curve up to occasion-level noise.
LCGA is the more parsimonious model and, in criminology especially, often the
preferred one; GMM is the more realistic and is what developmental psychology
and epidemiology usually publish. The two are nested, so the choice can be
made on BIC.
}
\examples{
\donttest{
set.seed(1)
n <- 400
cls <- rbinom(n, 1, 0.5)
icept <- ifelse(cls == 1, 3, 1) + rnorm(n, 0, 1)
slope <- ifelse(cls == 1, 1.0, 0.2) + rnorm(n, 0, 0.4)
y <- outer(icept, rep(1, 4)) + outer(slope, 0:3) + matrix(rnorm(n * 4, 0, 0.7), n, 4)
fit <- fit_gmm(y, times = 4, n_classes = 2, n_init = 10, random_state = 1)
fit
}
}
\references{
Muthen, B., & Shedden, K. (1999). Finite mixture modeling with mixture
outcomes using the EM algorithm. \emph{Biometrics}, \emph{55}(2), 463-469.
}
\seealso{
\code{\link[=fit_lcga]{fit_lcga()}} for the no-random-effects version and
\code{\link[=fit_rmlca]{fit_rmlca()}} for trajectory classes with no growth curve at all.
}
