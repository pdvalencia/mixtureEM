% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmlca.R
\name{fit_rmlca}
\alias{fit_rmlca}
\title{Repeated-Measures Latent Class Analysis}
\usage{
fit_rmlca(
  indicators,
  n_classes = 2,
  times = NULL,
  measurement = "binary",
  measurement_invariance = c("none", "full", "partial"),
  invariant_items = NULL,
  layout = c("time_major", "item_major"),
  id = NULL,
  time = NULL,
  items = NULL,
  item_names = NULL,
  time_labels = NULL,
  predictors = NULL,
  ...
)
}
\arguments{
\item{indicators}{The repeated indicators. Either a wide matrix or data frame
with \eqn{J \times T} columns (see \code{layout}), a three-dimensional array with
dimensions n by items by times, or a long data frame together with \code{id} and
\code{time}.}

\item{n_classes}{Integer. Number of latent classes.}

\item{times}{Integer. Number of occasions. Required for wide input; inferred
otherwise.}

\item{measurement}{Measurement model for a single occasion's items:
\code{"binary"}, \code{"categorical"}, \code{"continuous"}, or a named list for a mixed
block (as in \code{\link[=fit_mixture]{fit_mixture()}}).}

\item{measurement_invariance}{Whether the item parameters are held equal
across occasions. \code{"none"} (the default) estimates them separately at every
occasion, which is usually what you want here: the classes are patterns of
change, so forcing the items to behave identically over time can erase the
very differences being modelled. \code{"full"} holds every item equal, and
\code{"partial"} holds only the items named in \code{invariant_items}.}

\item{invariant_items}{Item indices or names held equal across occasions.
Used only when \code{measurement_invariance = "partial"}.}

\item{layout}{For wide input, whether columns run \code{"time_major"}
(all items of occasion 1, then all items of occasion 2, ...) or
\code{"item_major"} (all occasions of item 1, then all occasions of item 2, ...).}

\item{id, time}{For long input, the case and occasion identifiers, given
either as column names or as vectors.}

\item{items}{For long input, the columns to treat as indicators.}

\item{item_names, time_labels}{Optional display labels.}

\item{predictors}{Optional predictors of class membership. A grouping
variable (Collins and Lanza, sec. 7.2.1) is entered this way: a
multiple-group model and a model with the group as a dummy predictor are
equivalent when measurement is invariant across groups (their sec. 6.10.2).}

\item{...}{Further arguments passed to \code{\link[=fit_mixture]{fit_mixture()}}, such as \code{outcome},
\code{n_init}, \code{random_state}, \code{weights}, \code{strata} or \code{cluster}.}
}
\value{
An object of class \code{c("rmlca", "mixture_model")}. In addition to the
usual fields it carries \verb{$longitudinal}, holding the item and occasion
labels, the invariance specification and the wave-missingness pattern.
}
\description{
Fits a repeated-measures latent class model to data in which the same
indicators are observed at several occasions. Each person belongs to one
latent class for the whole study, so the classes describe \emph{trajectories}:
patterns of response that span the occasions rather than a snapshot at one of
them. This is Collins and Lanza's RMLCA (2010, sec. 7.2); with continuous
indicators the same model is Wang and Wang's longitudinal latent profile
analysis (2020, sec. 6.3.1), and it is obtained here simply by setting
\code{measurement = "continuous"}.

The likelihood is that of an ordinary latent class model applied to the
\eqn{J \times T} stacked indicators,
\deqn{P(y_i) = \sum_k \gamma_k \prod_t \prod_j \rho_{jtk}(y_{ijt}),}
so everything the package already offers — model selection, the bootstrap
likelihood-ratio test, predictors of class membership, distal outcomes,
survey designs and FIML for missing data — applies unchanged.

\code{measurement_invariance} controls whether the item-response parameters
\eqn{\rho_{jtk}} are held equal across occasions. Constraining them makes a
class label mean the same thing at every occasion and sharply reduces the
number of free parameters; leaving them free lets an item behave differently
over time. The two models are nested, so \code{\link[=longitudinal_lrt]{longitudinal_lrt()}} tests the
restriction directly.
}
\seealso{
\code{\link[=fit_lta]{fit_lta()}} for a model in which class membership may change
between occasions, and \code{\link[=longitudinal_lrt]{longitudinal_lrt()}} for testing invariance.
}
